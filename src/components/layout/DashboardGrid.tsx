import React from 'react'

const dashboardGrid = () => {
  return (
    <div>
      Dashboard grid would be rendered by Dashboard component. it contains individual components of KIP, Analytics, ReminderCard, RecentNotesCard, TaskStatuscard, HabitProgressCricle, TimeTracker
    </div>
  )
}

export default dashboardGrid
